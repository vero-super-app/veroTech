// address.dart
import 'package:flutter/material.dart';

class carhirepage extends StatelessWidget {
  const carhirepage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Car Hire'),
      ),
      body: const Center(
        child: Text('car hire page'),
      ),
    );
  }
}
