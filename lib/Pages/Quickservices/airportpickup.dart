// address.dart
import 'package:flutter/material.dart';

class Airportpickuppage extends StatelessWidget {
  const Airportpickuppage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Airport Pickup'),
      ),
      body: const Center(
        child: Text('airport pickup page'),
      ),
    );
  }
}
